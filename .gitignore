bin/
obj/
*.user

# Zips are GitHub Release assets. manifest.json is committed so Jellyfin can
# read it from raw.githubusercontent.com (GitHub's /releases/latest/download
# serves application/octet-stream, which the catalogue will not parse).
releases/

# macOS
.DS_Store
